import React from 'react';

const BodyWrapper = () => {
    const classes = {
        body: 'h-full w-full bg-red-100'
    };

    return <div className={classes.body} />;
};

export default BodyWrapper;
